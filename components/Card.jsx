import { StyleSheet, Text, View } from 'react-native'
import React, { useState } from 'react'

const Card = (id, name, discovered,) => {

    const [discovered, setDiscovered] = useState(false)

}

export default Card

const styles = StyleSheet.create({
    
})